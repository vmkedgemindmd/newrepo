{
  "name": "tomato-ai",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "npm run dev --prefix frontend & uvicorn api.index:app --reload",
    "start": "cd frontend && npm run preview"
  }
}
